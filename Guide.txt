1. ZIP
	zip -r folder.zip folder
	unzip file.zip

2. PIP3
	sudo apt-get install python3-pip
	
3.dpkg
	sudo dpkg -i <Đường dẫn tới file .deb>
	
	
4.git
	sudo git status : check trang thai thu muc da san sang commit hay chua
	
	
	
	sudo git add thumuc : add thu muc vao trang thai san sang commit (sudo git add -A : commit het)
	sudo git commit -m 'A MESSAGE DESCRIBING WHAT YOU’VE CHANGED'
	sudo git push -u origin master : push code len repository
		username :
		pass	 :
		
		
		
		
		
	sudo git clone YOUR_REPOSITORY_URL : sao chep repository tu github



